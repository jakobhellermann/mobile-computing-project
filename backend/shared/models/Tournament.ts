export type Tournament = {
  name: string;           //Tournament name
  eventType: string;      //Event Type (eg. online, offline)
  dateStart: string;      //Start of the Tournament
  dateEnd: string;        //End of the Tournament
  country: string;        
  region: string;         
  league: string;         
  prizepool: string;
  organizers: string;
  rulebook: string;
  overviewPage: string;   //OverviewPage of the Tournament
};
